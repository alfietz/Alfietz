# Alfietz - African Heritage Crafts Marketplace

## 🚀 Overview
Alfietz is a Vue 3 + Vite marketplace app for African heritage crafts. Buyers discover trending products, Suppliers showcase work. Features full auth flow, client-side libSQL database, responsive design (mobile/tablet/desktop), dark/light theme, localStorage persistence.

**Core User Types:**
- **Buyers**: Browse needs (e.g., 'Maasai beads necklace')
- **Suppliers/Sellers**: Showcase gives (e.g., 'Handcrafted Kente blazers')

## 📁 File Structure
```
alfietz/
├── src/
│   ├── App.vue*               # Central hub: routing, state, DB handlers
│   ├── db/client.js*          # libSQL Turso connection
│   ├── main.js                # Vue entry
│   ├── components/
│   │   ├── auth/              # Login/SignUp/Forgot/Verify/Reset flows
│   │   ├── shop/              # Home/ProductCard/SellerCard/UploadWork
│   │   ├── profile/           # Profile/EditProfile/Settings/Logout
│   │   └── layout/            # NavBar/WebHeader/Splash
│   └── assets/                # Logo/images
├── scripts/init-db.js*        # DB schema/seeding (⚠️ Destructive!)
├── errors.json*               # 50+ documented faults + project status
├── package.json*              # Vue 3.5 + libsql + vite 5
└── public/                    # Icons
```
\* = Critical files with known faults

## 🛠️ Setup & Run
```bash
# Install
npm install

# Dev server (localhost:5173)
npm run dev

# Optional: Init DB (⚠️ WIPES DATA)
node scripts/init-db.js

# Build
npm run build
```

**Env Vars (.env):**
```
VITE_TURSO_URL=your_turso_url
VITE_TURSO_AUTH_TOKEN=your_token
```

## ✨ Features
- ✅ Full routing (25+ screens)
- ✅ Responsive + Theme-aware UI
- ✅ Client-side DB (users/products/favorites)
- ✅ Buyer/Supplier roles + persistence
- ✅ Search + Trending (likes-based)
- ✅ WhatsApp deep-links

## ⚠️ Known Faults (50+ Documented)
See [errors.json](errors.json) for **complete fault catalog**:

**Critical:**
- DB connection crash (missing env vars)
- Data-wiping init script
- Offline libSQL failures

**High Impact:**
```
$ cat errors.json | jq '.faults[] | select(.severity=="high")'
```

## 📋 TODO from errors.json
```
✅ Updated errors.json (comprehensive faults)
✅ Generated README.md
⏳ Test npm run dev
⏳ Prioritize PWA + validation fixes
```

## 🎯 Next Steps (Prioritized)
1. **Fix DB connection** + offline support
2. **Add form validation** (vee-validate)
3. **PWA/ServiceWorker** for offline
4. **Error boundaries** + toasts (not alerts)
5. **Real Google Auth** + serverless backend

## 🏗️ Architecture Notes
- **State**: Global refs in App.vue + localStorage watchers
- **DB**: Client-side Turso libSQL (no backend)
- **Limits**: No real-time sync, offline fragile

**Production Roadmap**: Supabase/Firebase + real auth + PWA.

---

*Generated by BLACKBOXAI - All 50+ faults from every file analyzed*
